RewriteEngine On

# Serve existing files/directories directly (including /external/*)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Fallback all other routes to SPA entry
RewriteRule ^ index.html [L]
